{
  "name": "telegram-name-clock-weather-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "login": "node scripts/login.mjs",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "telegram": "^2.26.22"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.16.10",
    "@types/node": "^25.9.1",
    "input": "^1.0.1",
    "typescript": "^5.6.0",
    "vitest": "^4.1.7",
    "wrangler": "^4.95.0"
  }
}
